function addTwoNumbers(a, b) {
    // Convert inputs to integers (optional but safe)
    const num1 = parseInt(a);
    const num2 = parseInt(b);

    return num1 + num2;
}

// Example usage:
const result = addTwoNumbers(10, 20);
console.log("The sum is:", result);
